# AGENTS

## After Making A Change

- Run `npm run format:check`.
- Run `npm run typecheck`.
- Run `npm run lint`.
- Run all e2e with `npm run test:e2e`.
- If screenshots changed, regenerate them with `npm run test:e2e:update`, then rerun `npm run test:e2e`.
